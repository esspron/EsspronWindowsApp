<Window x:Class="EsspronAlcoholTester.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Esspron" 
        Height="500" Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Border Background="White" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <!-- Main Content -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Logo -->
                <Image Source="/Assets/esspronicon.png" 
                       Width="280" 
                       Height="80"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                
                <!-- Tagline -->
                <TextBlock Text="Alcohol Tester Manager" 
                           FontSize="16" 
                           Foreground="#64748B"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
                
                <!-- Loading Animation -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                    <Border x:Name="Dot1" Width="10" Height="10" CornerRadius="5" Background="#2563EB" Margin="4,0"/>
                    <Border x:Name="Dot2" Width="10" Height="10" CornerRadius="5" Background="#2563EB" Margin="4,0" Opacity="0.6"/>
                    <Border x:Name="Dot3" Width="10" Height="10" CornerRadius="5" Background="#2563EB" Margin="4,0" Opacity="0.3"/>
                </StackPanel>
                
                <TextBlock Text="Loading..." 
                           FontSize="13" 
                           Foreground="#94A3B8"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
            
            <!-- Version Info -->
            <TextBlock Text="Version 1.0.0" 
                       FontSize="11" 
                       Foreground="#CBD5E1"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Margin="0,0,0,20"/>
        </Grid>
    </Border>
</Window>
