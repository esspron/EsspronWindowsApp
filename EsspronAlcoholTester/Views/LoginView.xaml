<UserControl x:Class="EsspronAlcoholTester.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="480"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Side - Branding -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryGradientBrush}">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="60">
                    <!-- Logo -->
                    <Image Source="/Assets/esspronicon.png" 
                           Width="240" 
                           Height="70"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Alcohol Tester Manager" FontSize="20" 
                               Foreground="#E0E7FF" HorizontalAlignment="Center" Margin="0,8,0,40"/>
                    
                    <TextBlock Text="Professional breath alcohol testing&#10;data management solution"
                               FontSize="16" Foreground="#C7D2FE" 
                               TextAlignment="Center" LineHeight="28"/>
                    
                    <!-- Features List -->
                    <StackPanel Margin="0,50,0,0">
                        <StackPanel Orientation="Horizontal" Margin="0,10">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#4F46E5">
                                <TextBlock Text="✓" Foreground="White" FontSize="16"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="USB Device Auto-Detection" Foreground="White" 
                                       FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,10">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#4F46E5">
                                <TextBlock Text="✓" Foreground="White" FontSize="16"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Cloud Data Sync &amp; Backup" Foreground="White" 
                                       FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,10">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#4F46E5">
                                <TextBlock Text="✓" Foreground="White" FontSize="16"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Detailed Reports &amp; Analytics" Foreground="White" 
                                       FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,10">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#4F46E5">
                                <TextBlock Text="✓" Foreground="White" FontSize="16"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Multi-Device Management" Foreground="White" 
                                       FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                
                <!-- Footer -->
                <TextBlock Text="© 2024 Esspron Technologies" 
                           Foreground="#94A3B8" FontSize="12"
                           HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30"/>
            </Grid>
        </Border>
        
        <!-- Right Side - Login Form -->
        <Border Grid.Column="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel VerticalAlignment="Center" Margin="60,40">
                    
                    <!-- Sign In Form -->
                    <StackPanel x:Name="SignInPanel">
                        <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Sign in to manage your alcohol testing data" 
                                   FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,40"/>
                        
                        <!-- Email -->
                        <TextBlock Text="Email Address" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource ModernTextBoxStyle}" 
                                 Height="48"/>
                        
                        <!-- Password -->
                        <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,20,0,8"/>
                        <PasswordBox x:Name="PasswordBox" 
                                     Style="{StaticResource ModernPasswordBoxStyle}" 
                                     Height="48"
                                     PasswordChanged="PasswordBox_PasswordChanged"/>
                        
                        <!-- Remember Me & Forgot Password -->
                        <Grid Margin="0,16,0,0">
                            <CheckBox Content="Remember me" IsChecked="{Binding RememberMe}" 
                                      HorizontalAlignment="Left" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <Button Content="Forgot password?" Command="{Binding ForgotPasswordCommand}"
                                    HorizontalAlignment="Right" Background="Transparent" BorderThickness="0"
                                    Foreground="{StaticResource PrimaryBrush}" Cursor="Hand" Padding="0"/>
                        </Grid>
                        
                        <!-- Error Message -->
                        <Border Background="#FEE2E2" CornerRadius="8" Padding="16,12" Margin="0,20,0,0"
                                Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" FontSize="13"
                                       TextWrapping="Wrap"/>
                        </Border>
                        
                        <!-- Sign In Button -->
                        <Button Content="Sign In" Command="{Binding LoginCommand}"
                                Style="{StaticResource PrimaryButtonStyle}" 
                                Height="50" Margin="0,24,0,0"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
                        
                        <!-- Loading Indicator -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0"
                                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                            <Ellipse Width="20" Height="20" StrokeThickness="3" Stroke="{StaticResource PrimaryBrush}">
                                <Ellipse.RenderTransform>
                                    <RotateTransform x:Name="LoadingRotation" CenterX="10" CenterY="10"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <TextBlock Text="Signing in..." Margin="10,0,0,0" VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                        
                        <!-- Contact Admin Note -->
                        <Border Background="#F1F5F9" CornerRadius="8" Padding="16,12" Margin="0,30,0,0">
                            <TextBlock Text="Don't have an account? Contact your administrator to get access." 
                                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"
                                       TextWrapping="Wrap" TextAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
